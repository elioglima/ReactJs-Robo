import * as Applications from '../../../src/applications'
import * as Routes from '../../../../routes/routes_actions'
export const logadoInicio = (e) => dispatch => { dispatch(Routes.logadoInicio(e)) }